# Source-Native integration

The Source-Native collector and models may be migrated into this project later through explicit public interfaces.

The first public integration points are:

- Observation schema;
- provider adapter contract;
- public readiness status;
- observer output contracts;
- State Certificate publisher.

Private raw Alpaca SIP payloads and credentials should remain outside the public repository unless licensing and security review explicitly permit publication.
