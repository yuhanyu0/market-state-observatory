# Model Shadow

Model Shadow begins only after the Data Shadow gate passes.

The model emits frozen predictions and paper-only actions using point-in-time data. It never routes a real order. Direction, Transmission, Episode and Playbook arms are tracked independently so that incremental value can be identified.

Passing Model Shadow does not authorize material capital. A later micro-live stage would require a separate risk policy and explicit approval.
