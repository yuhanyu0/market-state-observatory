# Theme Radar Observer Bridge

Theme Radar is external and optional. Its bridge transports a versioned
structural-attention observer estimate only. The public Market State Observatory
does not import the old repository, logs, runtime, or private outputs.

The adapter cannot produce Direction, candidate themes, decision gates,
playbooks, vehicles, positions, or orders.
