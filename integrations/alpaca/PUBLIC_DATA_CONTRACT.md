# Alpaca Public Data Contract

Input must pass `rehearsal_summary.schema.json`, contain no forbidden secret or
account fields, report `counts_toward_model_shadow=false`, and report both paper
and real order permissions as false for a rehearsal. The bridge publishes only
aggregate, already-redacted evidence.

The 2026-08-10 Day 0B record establishes SIP data-capture readiness for the
afternoon chain. It does not establish bullish Direction, Transmission state,
Episode state, alpha, strategy eligibility, or permission to trade.
