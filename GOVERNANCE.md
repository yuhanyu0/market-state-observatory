# Governance

The project currently uses a maintainer-led model.

Architecture changes should be recorded as Architecture Decision Records in `docs/adr/`. Changes to public schemas require a schema-version increment and a migration note. Research conclusions cannot be promoted to action gates solely by editing documentation; the corresponding validation evidence must be recorded.
